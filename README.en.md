# Realtime Narration Video

English | [日本語](README.md)

> A fully local-capable, full-frame conversational character pipeline built on the general-purpose LTX video diffusion model. The best observed time from user submission to the first completed video was approximately 2.6 seconds on an RTX 5090-class test environment.

This proof-of-concept application sends chat input to an OpenAI-compatible Gemma 4 endpoint, converts the streaming response to speech with AivisSpeech, and feeds approximately five-second audio chunks to LTX-2.5. Completed videos are played in sequence. Audio generated by LTX is discarded and the original TTS waveform is muxed into the final MP4.

See [Development History and Measurements](docs/development-notes.en.md) for the latency, resolution, step-count, and photorealistic lip-motion experiments. See the [Technical Guide](docs/technical-guide.en.md) for the architecture, API, parameters, and operations.

## Demo

[![Realtime Narration Video demo](docs/assets/demo-preview.jpg)](docs/assets/demo.mp4)

Click the image to open a 57-second demonstration of streaming LLM output being converted to speech and video. You can also open the [MP4 directly (1.8 MB)](docs/assets/demo.mp4).

The video was compressed for GitHub and therefore looks slightly worse than the original generated and displayed output.

## Current MVP

- Sentence/clause segmentation and sentence-level TTS
- Streaming chat with conversation history through an OpenAI-compatible Gemma 4 endpoint
- TTS starts for finalized clauses while the LLM response is still streaming
- Dynamic video chunks that favor punctuation and begin TTS around 22–26 Japanese characters while protecting word endings
- Pipelined TTS audio preparation and LTX video generation
- Drag-and-drop character images and TXT files
- Chunk assembly based on actual WAV duration
- LTX-2.5 Audio-to-Video generation
- The same 16/20/24 fps landscape, 4:3, and portrait profiles as Realtime Video Studio
- A generated speaking anchor is reused for every photorealistic chunk to avoid a closed-mouth reference
- During character registration, LTX is preloaded and a high-quality open-mouth anchor is generated at the selected resolution with eight steps
- Original TTS audio replaces the LTX-generated audio
- Playback starts with one available chunk; two alternating video elements preload the next clip
- Dynamic startup buffering and measured transition-gap display
- Short utterances are generated as five-second clips. Playback retains the silent tail while the next clip is pending, but skips it once the next clip is ready
- The first clip of every turn uses a lower resolution with the same framing and four steps; later clips use the selected resolution and eight steps
- Seed 1004, the most reliable observed photorealistic articulation seed, is used for every photorealistic chunk
- Photorealistic fast mode uses `modality_scale=1.3` only while preparing the anchor; Strong Lip Motion also applies it during conversation
- At the beginning of each turn, the original character image covers the previous turn until the new video starts
- User-selectable generation policy: Standard uses variable seeds and within-turn frame chaining; Photorealistic reuses the speaking anchor and seed 1004
- Gateway jobs are polled every 100 ms
- SSE status updates and LLM/TTS/LTX timestamps are recorded
- Gateway and TTS errors are shown and persisted

On a single GPU, generation can occasionally be slower than playback. When the buffer is exhausted, the application retains the current clip until the next one is ready; this is therefore a near-real-time system rather than a hard real-time guarantee.

## Indicative Hardware Configurations

| GPU configuration | Assessment |
|---|---|
| RTX 5090 32 GB alone, with the TE (Text Encoder) moved off GPU 0 | Excellent headroom (GPU 0: approximately 18 GB) |
| 24 GB Blackwell + 16 GB | Good headroom; speed still requires measurement |
| 16 GB Blackwell + any-generation 16 GB GPU | Marginal: approximately 0 GB headroom after all memory reductions; emulated validation required |
| RTX 4090 24 GB | Unsupported by the FP4 kernel; NF4 can run but is slower |

These are estimates for the FP4 configuration used by this project. Actual memory consumption and speed vary with the video profile, model placement, concurrent processes, and driver environment.

## Running

Requires Python 3.11 or newer, FFmpeg, a running diffusers-movie-server Gateway, and AivisSpeech Engine.

```bash
python3 -m venv .venv
.venv/bin/pip install -e '.[dev]'
cp .env.example .env
PATH="$PWD/.venv/bin:$PATH" ./run.sh
```

Open `http://localhost:8782` in a browser.

Configure the LLM, Gateway, and TTS endpoints in `.env`. The included `.env.example` uses localhost and should be adapted to your environment. If `LLM_MODEL` is empty, the application queries `/v1/models`, prefers a model whose name contains `44b`, and otherwise uses the first model.

The Gateway manages backends exclusively. Unmanaged H3/LTX processes on ports 8631/8632 can cause generation requests to return HTTP 409.

## API

- `POST /api/sessions` — submit the character, text, concept, and settings as multipart data
- `POST /api/sessions/{id}/messages` — submit a user message and start streaming generation
- `GET /api/sessions/{id}` — retrieve session and chunk status
- `DELETE /api/sessions/{id}` — cancel after the current chunk finishes
- `GET /api/sessions/{id}/chunks/{index}/video` — retrieve the MP4 with replaced audio
- `GET /api/sessions/{id}/chunks/{index}/audio` — retrieve the original TTS WAV
- `GET /healthz` — application, Gateway, and TTS health

## Tests

```bash
.venv/bin/pytest -q
```

## License

Application code in this repository is released under the [Apache License 2.0](LICENSE).

Python packages, FFmpeg, AivisSpeech, LTX-2.5, Gemma, other dependencies, external services, models, and model weights are not covered or relicensed by this project's license. Follow the licenses, terms of use, and distribution conditions of their respective providers. No model or model weights are included in this repository.

### Commercial use

If you plan to use this project commercially, please contact us through [GitHub Issues](https://github.com/animede/Realtime_Narration_Video/issues) so that we can understand deployment use cases and offer technical assistance where appropriate.

This is a voluntary request for coordination and does not add a condition to or restrict commercial use permitted by the Apache License 2.0. Separately verify whether commercial use is permitted for every dependency, model, weight, and external service you use.
